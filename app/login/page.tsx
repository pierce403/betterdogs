'use client'; import { auth, googleProvider } from '@/lib/firebaseClient'; import { signInWithEmailAndPassword, signInWithPopup } from 'firebase/auth'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function P(){const [e,setE]=useState('');const [p,setP]=useState('');const r=useRouter();return <form className='space-y-2' onSubmit={async x=>{x.preventDefault();await signInWithEmailAndPassword(auth,e,p);r.push('/dashboard')}}><input placeholder='Email' className='border p-2 w-full' onChange={x=>setE(x.target.value)}/><input type='password' placeholder='Password' className='border p-2 w-full' onChange={x=>setP(x.target.value)}/><button className='bg-black text-white px-4 py-2'>Login</button><button type='button' className='border px-4 py-2 ml-2' onClick={async()=>{await signInWithPopup(auth,googleProvider);r.push('/dashboard')}}>Google</button></form>}
