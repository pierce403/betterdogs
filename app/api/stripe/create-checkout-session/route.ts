import { NextResponse } from 'next/server'; import Stripe from 'stripe'; import { requireAuth } from '@/lib/auth-server';
export async function POST(){try{const u=await requireAuth(); if(!process.env.STRIPE_SECRET_KEY||!process.env.STRIPE_PRICE_COACH_MONTHLY) return NextResponse.json({message:'Stripe not configured'},{status:400}); const stripe=new Stripe(process.env.STRIPE_SECRET_KEY); const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price:process.env.STRIPE_PRICE_COACH_MONTHLY,quantity:1}],success_url:`${process.env.NEXT_PUBLIC_APP_URL}/dashboard`,cancel_url:`${process.env.NEXT_PUBLIC_APP_URL}/pricing`,client_reference_id:u.uid});return NextResponse.json({url:session.url});}catch{return NextResponse.json({error:'Unauthorized'},{status:401});}}
